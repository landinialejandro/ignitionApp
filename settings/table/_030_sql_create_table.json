{
    "caption": "sql to create table",
    "properties": [{
        "placeholder": "create table sql",
        "value": "create table if not exists `{{databaseName}}`.`{{tableName}}` ( {{fieldsDefinitions}} ) CHARSET {{charset}}"
    }],
    "type": "input-group",
    "icon": {
        "value": "bi bi-type",
        "type": "class"
    },
    "order": 30                                                                     
}