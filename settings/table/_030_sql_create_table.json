{
    "caption": "sql to create table",
    "properties": {
        "placeholder": "create table sql",
        "caption": "create table if not exists `{{databaseName}}`.`{{tableName}}` ( {{fieldsDefinitions}} ) CHARSET {{charset}}"
    },
    "type": "tbl-setting",
    "icon": "fa fa-database",
    "order": 30
}