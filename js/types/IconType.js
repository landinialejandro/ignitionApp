/**
 * @typedef {Object} IconType
 * @property {string} type - Identifica el tipo de icono a mostrar, puede ser una clase, SVG o definido por el usuario.
 * @property {string} value - El valor del icono, puede ser una clase o código SVG.
 */

// Exportar la definición de IconType
export { };
